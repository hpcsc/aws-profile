#!/usr/bin/env expect -f

set send_slow {1 .1}
proc send {ignore arg} {
	sleep .1
	exp_send -s -- $arg
}

set timeout -1
match_max 100000

spawn ./aws-profile set --credentials-path ./e2e/tmp/credentials --config-path ./e2e/tmp/config

send -- "j"
send -- "\r"

expect eof
